# X Scrape Report — 2026-03-26

**Scrape Time:** 2026-03-26 18:03 IST  
**Search Queries Attempted:**
1. `neurointervention OR thrombectomy OR #Neurointervention OR #stroke` (top, today)
2. `cerebral AVM OR intracranial aneurysm OR endovascular` (top, today)

## Result: ❌ FAILED

### Reason
X/Twitter requires authentication to view any content. When navigating to the search URL, X redirected to a login/signup page ("Happening now — Join today"). The OpenClaw browser profile does not have X credentials stored.

**Attempted browser profiles:**
- `openclaw` — Not logged in to X (showed login wall)
- `user` — Chrome MCP couldn't connect (Chrome not running or DevTools unavailable)

### Posts Extracted
None — could not access content behind login wall.

### Database
No records written to `memory_x_posts.db` (scrape aborted before data collection).

### Action Required
To enable X scraping, please either:
1. Log in to X in the OpenClaw Chrome profile (start Chrome with `--user-data-dir` pointed at the OpenClaw profile)
2. Or ensure the `user` Chrome profile is running with active X session and re-run the scrape
3. Alternatively, use the `xurl` skill if X API credentials are configured

### Summary
- **New posts found:** 0
- **High-engagement posts (>50 likes):** N/A
